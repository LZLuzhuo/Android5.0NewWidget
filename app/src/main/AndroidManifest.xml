<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="me.luzhuo.android50newweight">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activity.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".activity.ToolBarDemo"/>
        <activity android:name=".activity.CardViewDemo"/>
        <activity android:name=".activity.RecyclerViewDemo"/>
        <activity android:name=".activity.RecycleViewItemDemo"/>
        <activity android:name=".activity.SwipeRefreshLayoutDemo"/>
    </application>

</manifest>